spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cocktail_dev?useSSL=false
    username: root
    password:
    testWhileIdle: true
    validationQuery: SELECT l

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      dialect: org.hibernate.dialect.MySQL5Dialect